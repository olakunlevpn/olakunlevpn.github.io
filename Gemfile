source "https://rubygems.org"

# GitHub Pages build environment (includes Jekyll, jekyll-feed,
# jekyll-remote-theme, jekyll-sitemap, jekyll-paginate).
gem "github-pages", group: :jekyll_plugins

# Local preview: bundle exec jekyll serve
